
/* 
✅What is an Array?

An array is a special object that stores multiple values in a single variable.

Instead of writing:

let student1 = "Raju";
let student2 = "Rahim";
let student3 = "Karim";

Use an array:
let students = ["Raju", "Rahim", "Karim"];


✅Why Do We Need Arrays?

Suppose you have the marks of 100 students.

Without an array:

let mark1 = 80;
let mark2 = 75;
let mark3 = 90;
// ...

With an array:
let marks = [80, 75, 90];

One variable can hold many values.


✅Creating an Array

Numbers
let nums = [10, 20, 30, 40];

Strings
let fruits = ["Apple", "Banana", "Mango"];

Mixed Data Types
let data = [10, "Raju", true, null];


✅Array Index

Each element has an index.

Index :   0      1      2
Value : ["A",  "B",  "C"]

The first element is always at index 0.




✅✅Common Array Methods

| Method       | Purpose                 |
| ------------ | ----------------------- |
| `push()`     | Add to end              |
| `pop()`      | Remove from end         |
| `shift()`    | Remove from beginning   |
| `unshift()`  | Add to beginning        |
| `includes()` | Check if value exists   |
| `indexOf()`  | Find index              |
| `join()`     | Convert array to string |
| `sort()`     | Sort elements           |
| `reverse()`  | Reverse order           |
| `length`     | Number of elements      |












*/